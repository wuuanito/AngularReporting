import { FormatoDatePipe } from './formato-date.pipe';

describe('FormatoDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoDatePipe();
    expect(pipe).toBeTruthy();
  });
});
